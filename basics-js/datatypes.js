"use strict"; // treat all JS code as newer version

// alert(3+3) // we are using node.js, not browser

console.log(3
    +3) // code readability should be high i.e indentation
console.log(" Nishant");

let name = 'Nishant'
let age = 22
let isLoggedIn = false
let state = null
let city

// Primitive DataTypes
//number => 2^53
//bigint => use when have to store large number/integer value
//string =>
//boolean => true/false
//null => standalone value
//undefined => value is not assigned
/*symbol => unique(when we make multiple components
 and we have to identfiy which one is unique)*/

// object
console.log(typeof(null)); // Output and type is object
console.log(typeof(undefined)); //Output and type is undefined  


